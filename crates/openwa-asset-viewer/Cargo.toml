[package]
name = "openwa-asset-viewer"
version = "0.1.0"
edition = "2024"
description = "Immediate-mode debug UI for OpenWA (egui floating window)"

[dependencies]
openwa-core = { path = "../openwa-core" }
eframe = { version = "0.28", default-features = false, features = [
    "glow",
    "default_fonts",
] }
rfd = "0.15"
